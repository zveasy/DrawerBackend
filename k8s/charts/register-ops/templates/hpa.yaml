apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: register-ops
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: register-ops
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
